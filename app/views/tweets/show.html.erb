<h1>口コミ投稿</h1>
<h3>投稿詳細</h3>
<div class="tweet">
  <p><%= @tweet.body %></p>
  <p><%= @tweet.created_at %></p>
</div>
  <%= link_to "編集する", edit_tweet_path(@tweet.id) %>
  <%= link_to "投稿一覧に戻る", tweets_path %>
